import math
def main():
#we are finding all the even numbers and square numbers from 1 to 100
    number = (100)
    for i in range(0,number):
        if i % 2 == 0 :
#
            evenNum = ("These are the even numbers", i)
            #print(evenNum)

#import math allows you to do the square root of a number
        for a in range(0,number):
            print(a)
            a = math.sqrt(a)
            if a % 1 == 0:
                print(a)


main()